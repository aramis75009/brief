# --- Secrets / env -----------------------------------------------------------
.env
.env.*
!.env.example
!.env.*.example

# --- Dépendances -------------------------------------------------------------
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# --- Build / cache -----------------------------------------------------------
.next/
out/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo
next-env.d.ts

# --- Vercel ------------------------------------------------------------------
.vercel

# --- Logs --------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# --- Divers ------------------------------------------------------------------
*.pem

# --- OS / éditeur ------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# NB : `vercel link` ré-ajoute parfois `.env*` ici. Ne pas le laisser : placée en
# dernier, cette règle écrase les négations `!.env.example` du bloc Secrets en
# haut du fichier et rend le template non committable. Le bloc du haut suffit.
.gstack/

# Données serveur locales (abonnements push, avant Postgres)
.data/
