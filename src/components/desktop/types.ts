/** Écrans internes de la version desktop — jamais mélangés avec `Screen` (mobile). */
export type DesktopScreen = "dashboard" | "calendrier" | "tâches" | "kanban" | "objectifs" | "détail" | "idées" | "réglages" | "graphe";
